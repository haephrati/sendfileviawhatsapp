# sendfileviawhatsapp

MFC helper (`SG_SendWhatsApp`) for sending a WhatsApp message with an optional file attachment.

Open `SG_SendWhatsAppFile.sln` in Visual Studio.

## Unit tests

`Tests/SendWhatsAppTests` checks about, main dialog, mainframe, and icon resource IDs. Build Release|x64.
